# Data

Place the raw loan dataset here as:

```text
Data/loan_data.csv
```

Raw data files are intentionally ignored by Git. This keeps the repository
lightweight and avoids publishing data without an explicit decision.
